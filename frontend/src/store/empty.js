/* eslint-disable */
import * as types from '../mutation-types'

const state = {
}

// actions
const actions = {
}

// mutations
const mutations = {
}

// getters
const getters = {
}

export default {
  state,
  getters,
  actions,
  mutations
}
